const TEST_TIMESTAMP = 1542836053153
const TEST_TIME_HOURS = 21
const TEST_TIME_MINUTES = 34
const TEST_TIME_SECONDS = 13
const TEST_TIME_MILLISECONDS = 153
const TEST_NEXT_UPDATE_INTERVAL = 46847

export default {
  test: {
    timestamp: TEST_TIMESTAMP,
    time: {
      hours: TEST_TIME_HOURS,
      minutes: TEST_TIME_MINUTES,
      seconds: TEST_TIME_SECONDS,
      milliseconds: TEST_TIME_MILLISECONDS
    },
    nextUpdateInterval: TEST_NEXT_UPDATE_INTERVAL
  }
}
